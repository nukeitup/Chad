{
  "version": 2,
  "installCommand": "npm ci --include=dev && npx prisma generate",
  "buildCommand": "mkdir -p public && echo 'API Server' > public/index.html",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/:path*", "destination": "/api" }
  ]
}
